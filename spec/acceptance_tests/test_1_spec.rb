# Test 1: It will display a start game message and instructions.
# Given: The user starts a game.
# When: When the user wants to start the game on the command line
# Then: A welcome and instructions will appear.

describe "user starts a game" do
    context "at the start of the game" do
        xit "displays a welcome message" do
            # a new game is initialised, displays a welcome message
        end

        xit "displays instructions" do
            # displays instructions on a new line after welcome message
        end
    end
end